# 研究室配属システム 設計メモ

## 1. 目的

学生の希望順位・GPA・研究室ごとの教員裁量点から、安定マッチング（研修医-病院問題）
によって研究室配属を決定する Web アプリケーション。

- 静的 HTML + JS（Vite でビルドした `dist/` を配信。サーバ不要、データは端末外に出ない）
- 入力ファイルをドラッグ＆ドロップ → パラメータ調整 → 実行 → 結果表示 → CSV/Excel ダウンロード
- マッチングは [`hospital-resident-matching`](https://github.com/dfukagaw28/hospital-resident-matching) の
  `stableMatch()`（学生側最適の受入保留アルゴリズム）を使う

## 2. 用語の対応

| Hospital/Resident 問題 | 本システム |
| --- | --- |
| resident | 学生 |
| hospital | 研究室 |
| capacity | 定員 |
| resident preferences | 学生の希望順位（全研究室を順位づけ） |
| hospital preferences | 総合点（GPA点 + 教員裁量点）の降順 |

学生側最適の DA を使うため、学生にとって正直申告が最適戦略になる（耐戦略性）。

## 3. データフロー

```
[eClass 希望順位ファイル (.txt)] ─┐
[GPA ファイル (.xlsx)]        ─┼─→ パーサ層 ─→ 正規化データ ─┐
[裁量点ファイル (.xlsx) × 研究室数] ─┘                      │
                                                            ↓
                          パラメータ（重み・同点処理・定員上書き 等）
                                                            ↓
                          スコア計算 → 研究室の選好リスト生成
                                                            ↓
                                   stableMatch()
                                                            ↓
                          結果集計（希望充足率・安定性検証）
                                                            ↓
                              画面表示 / CSV・Excel 出力
```

パーサ層とドメイン層を分離するのが要点。ファイル形式が変わってもドメイン層は無傷。

## 4. 正規化データモデル（中間表現）

```ts
type StudentId = string;
type LabId = string;

interface Student {
  id: StudentId;          // 学籍番号
  name?: string;
  gpa: number;            // 素点（換算前）
  preferences: LabId[];   // 第1希望から順に
}

interface Lab {
  id: LabId;
  name?: string;
  capacity: number;
  scores: Map<StudentId, number>;  // 教員裁量点（素点）
}

interface Instance {
  students: Student[];
  labs: Lab[];
}
```

### 暫定の入力形式（M2）

実ファイル（eClass のテキストと Excel）のパーサができるまで、こちらで決めた CSV を
読む。M3 のパーサも「テキスト → 中間の行」という同じ形で書き、`io/intake.ts` から
差し替える。中間表現より手前だけが変わるので、ドメイン層は影響を受けない。

| 役割 | 形式 |
| --- | --- |
| 希望順位 | `学籍番号,氏名,第1希望,第2希望,…`（1 ファイル） |
| GPA | `学生ID,学生氏名,GPA` の Excel（1 ファイル。名簿を兼ねる） |
| 研究室・定員 | `研究室,研究室名,定員,選択肢ラベル`（1 ファイル。`研究室` 以外は任意） |
| 教員裁量点 | 教員ごとの Excel `学生ID,学生氏名,教員氏名,教員裁量点`、または `研究室,学籍番号,裁量点` |

入力ごとに研究室の呼び方が違うので、研究室 CSV がそれを繋ぐ。eClass の希望順位は
`○○研究室（○○　○○）` のような表示名で指すので `選択肢ラベル` 列、教員ごとの裁量点
Excel は `○○　○○` のような教員氏名で指すので `教員氏名` 列。研究室 ID・研究室名・
選択肢ラベル・教員氏名のどれからでも引き、空白の入れ方の違いは無視する
（`io/intake.ts` の `labLookup`）。

### 名簿の突き合わせ

GPA のファイルと教員ごとの裁量点のファイルは、同じ学生を並べたもので行数も揃う。
揃っていなければどちらかが古いか、行が消えている（`io/intake.ts` の `checkRoster`）。

見落とすと痛いのは、教員の表から学生の行ごと消えている場合。点数が空欄なら読み取りが
止まるが、行が無ければ `missingScore` の既定では 0 点として扱われ、その学生はその
研究室では黙って最下位近くに落ちる。

### Excel

`.xlsx` は zip に XML を詰めたものなので、`fflate` で開いて必要な XML だけ読む
（`io/xlsx.ts`）。SheetJS を入れなかったのは、npm の `xlsx` が 0.18.5 で止まっていて
既知の脆弱性があり、本体は 400KB あるのに対し、要るのが「シートを行と列にする」
だけだから。解凍ライブラリは Excel が落とされたときに初めて取りに行く（動的 import）。

共有文字列の `<si>` にはふりがなの `<rPh>` が入っていることがあり、一緒に読むと
「教員氏名キョウインシメイ」のような値になる。本文の `<t>` と `<r><t>` だけを読む。

パーサは行と列（`string[][]`）を受け取る形に揃えてあり、CSV から起こしても Excel の
シートから起こしても同じ処理に流れる（`SourceFile.rows`）。

見出しには別名を許す（`学籍番号` / `学生番号` / `ID`、`裁量点` / `点数` / `得点` など）。
文字コードは BOM と UTF-8 として解釈できるかどうかで判別し、駄目なら Shift_JIS と
みなす（Excel が書き出す CSV の多くがこれ）。

**定員は研究室 CSV から取る**ことにした（未確定事項 3 への暫定の答え）。ただし列は
任意で、無ければ**合計がちょうど学生数になるように配分**する（`domain/capacity.ts` の
`tightCapacities`）。全研究室に「学生数 ÷ 研究室数」の切り捨てを配り、余った席を
第 1 希望に挙げた学生が多い研究室から 1 つずつ、同数なら第 2 希望の数、それでも
並ばなければ抽選で決める。ライブラリの `setCapacities(-1, true)`（tight）と同じ方式で、
`popularityKeys` と `compareKeys` をそのまま借りている。

数えるのは学生が自分で書いた希望順位表。補完で足した分まで数えると、誰も挙げていない
研究室が人気であるかのように見えてしまう。

定員は運用上の決定なので、どう決めたかを警告に出す。一部の研究室だけ空欄なのは
書き忘れとみて止める。

サンプルは `samples/` にある（匿名の合成データ）。

### eClass の希望順位ファイル（M3）

eClass が書き出すテキスト。CSV だが、頭に見出しが付き、`[...]` で始まるブロックが
空行で区切られて並ぶ。

```
1〜5 行目   ヘッダ（教材名、出力日時など）— 使わない
            出力時のオプションによって 6 行になることがある
（空行）
（2 行）    問題のパラメータ（CSV 2 行）
            列の後半に選択肢ラベル option1 … option26 が入る
            ラベルは <p> タグ付きの HTML で、中のテキストだけを使う
9 行目      空行
            [回答一覧]                              …ブロック
            [アンケート集計]                        …ブロック
            [ユーザ毎の回答リスト]                  …ブロック（これがメイン）
            [ユーザ毎の回答時間リスト(単位:秒)]     …ブロック
            [回答数リスト（表形式の設問のみ）]      …ブロック
```

学生ごとの希望順位は `[ユーザ毎の回答リスト]` から取る。選択肢の番号
（`option1` など）を研究室に対応づけるのがパラメータ行のラベル。

同じ見出しのブロックが複数あれば**最初のものを使う**。配属に使うアンケートは設問が
一つなので、これで足りる。取りこぼしに気づけるよう、見出しが繰り返されていたら
`sections.notes` で知らせる（`npm run inspect` が「注意」として表示する）。

回答は `<設問1/設問2/設問3/・・・>` の 1 列にまとまっていて、`4, 1, 3, 2` のように
**並び順が順位、値が選択肢の番号**を指す（この例なら 1 位が option4、2 位が option1）。
`未解答` が混じったらその順位を飛ばし、後ろを繰り上げる。全部 `未解答` の学生は
希望なしとして通す（下の「不完全な希望順位表」で全研究室がランダムな順になる）。

引用符の中に改行やカンマを含む欄（HTML のラベル）があるので、行に切ってから
CSV にするのではなく、ファイル全体を CSV として読んでから行を数える
（`io/eclass.ts` の `splitSections`）。

行数は決め打ちにせず、空行と `[...]` の見出しで区切って数える。ヘッダの行数は出力
オプション次第で 5 行にも 6 行にもなるので、どちらも黙って通す。それを外れた行数や、
パラメータが 2 行でない場合は `sections.notes` に書き出すだけで止めはしない
（`npm run inspect` が「注意」として表示する）。止まるのはパラメータの行が一つも
無いときだけで、それはこのパーサの知っている形ではないということ。

ブロックの見出しは 1 列目の頭が `[...]` かどうかだけで決める。実ファイルの見出しには
余分な欄が付いていることがあり、`[解答の正否リスト](最新結果のみ表示しています)` の
ように角括弧の後ろに但し書きが続くこともある。厳しく見ると見出しを取りこぼし、その
ブロックが次のブロックと繋がってしまう。但し書きは `block.remark` に取っておくので、
`findBlock` は素の名前で引ける。

## 5. スコアリング

研究室 h における学生 s の総合点:

```
total(s, h) = gpaPoint(s) + discretionaryPoint(h, s)

gpaPoint(s)            = s.gpa / gpaMax * gpaWeight        // 既定 gpaWeight = 40
discretionaryPoint(h,s) = h.scores.get(s.id)               // 既定 60 点満点
```

GPA は 4.0 満点として 40 点に換算する（`gpaMax` の既定値が 4.0）。別の尺度で運用する
年があれば、画面の「GPA の満点」を変えれば足りる。

研究室 h の選好リスト = 全学生を `total(s, h)` の降順に並べたもの。

### 不完全な希望順位表

学生が全研究室に順位を付けるとは限らない（`未解答`、途中まで、まったく無し）。
eClass に回答しなかった学生も、順位を一つも付けていない学生として同じ扱いにする。
名簿は GPA のファイルで、そこに載っていて希望順位に無い学生がそれにあたる。
解く前に、順位を付けた研究室をそのままの順で上位に置き、**残りの研究室をランダムな
順で下位に足して**、全研究室を並べた表にする。乱数は抽選と同じシードから決まる
（流れは分けてある）ので、同じシードなら同じ並びになる。学籍番号と研究室を辞書順に
整列してから引くので、入力ファイルの行順にも依らない。

持つのは足した分（`Assignment.preferenceRest`）だけで、繋いだ表は使うところで作る
（`completedPreferences`）。学生自身の表は `Student.preferences` にあるので、繋いだ表を
持つと二重に抱えることになり、どこまでが学生の書いた分かも読めなくなる。ライブラリが
`residentPrefs` と `residentPrefsRest` を分けて持つのと同じ形。

これで定員の合計が学生数以上なら未配属は出ない。代わりに「自分で順位を付けていない
研究室に配属された学生」が出るので、結果ではそれを**希望外**として区別する
（`StudentRow.listed`、`Summary.unlisted`）。希望順位の内訳は学生が自分で付けた
順位だけを数え、補完で足した分は第 N 希望に数えない。

安定性の検査は補完後の表で行う。自分で付けた分だけで見ると、希望外に配属された学生が
すべての研究室を今より好むことになり、ありもしないブロッキングペアが並ぶ。

### 乱数の流れ

シードから引く乱数は用途ごとに種をずらして別の列にする（`domain/streams.ts`）。
同じ種から二つ引くと、抽選の結果と補完の結果が同じ乱数列に乗ってしまう。

| 用途 | ずらし幅 |
| --- | --- |
| 同点処理の抽選番号 | 0 |
| 順位を付けなかった研究室の並べ方 | 1 |
| 定員の余り一席の行き先 | 2 |

### 同点処理: 抽選番号

DA は厳密な全順序を要求するため、総合点が同点の学生を区別する規則が要る。
本システムは**抽選**による。学籍番号順のような決定的な規則は、番号の若い学生が
恒常的に有利になるため採用しない。

全学生に 0 から始まる**抽選番号**を一つ与え、総合点が同点のときは抽選番号の
小さい学生を優先する。研究室ごとに引き直すのではなく全研究室で共通の番号を使う
（single tie-breaking）。理由:

- 「抽選番号 N 番」として学生に開示でき、結果を監査できる
- 学校選択制の研究（Abdulkadiroğlu–Pathak–Roth 2009）で、研究室ごとに引き直すより
  学生の希望充足が良いことが知られている

抽選番号の決め方:

```
学籍番号を辞書順に整列 → シードから作った乱数順列で並べ替え → その順位が抽選番号
```

入力ファイルの行順に依存しないので、同じシード・同じ名簿なら常に同じ結果になる。
乱数はライブラリの `Pcg32Rng`（PCG32）と `permutation`（Fisher-Yates）を使う。

シードは画面から入力できるパラメータとし、**ページ読み込み時にランダムな値を既定値
として設定する**。実行結果には必ずシードを記録し、後から同じ結果を再現できるようにする。

## 6. 調整可能パラメータ（UI）

| パラメータ | 既定 | 備考 |
| --- | --- | --- |
| GPA 配点 | 40 | 合計 100 点になるよう裁量点配点と連動 |
| 裁量点 配点 | 60 | |
| GPA の満点（換算元） | 要確認 | 4.0 スケールか等 |
| 抽選シード | 読み込み時にランダム | 同点処理に使う。結果に必ず記録する（5 節） |
| 定員 | 研究室 CSV、無ければ合計がちょうど学生数になるよう配分 | 画面から上書きできるようにしたい |
| 裁量点が無い学生の扱い | 0 点として扱う / 受入不可 | **要決定** |
| 希望の考慮上限 | 全研究室 | 第 k 希望までに絞る場合 |

## 7. 出力

- **学生別**: 学籍番号 / 氏名 / 配属研究室 / 第何希望 / その研究室での総合点・順位
  （研究室ごとにまとめ、研究室の中は学生 ID 順。未配属は最後）
- **研究室別**: 研究室 / 定員 / 配属人数 / 配属学生一覧（点数順）
- **サマリ**: 第1希望充足率、希望順位の分布、未配属者数
- **検証**: ブロッキングペアが存在しないこと（安定性の自己検査）

形式は CSV（依存なし）と Excel（SheetJS で読み書き）。入力に .xlsx がある以上、
Excel ライブラリはどのみち必要なので出力も .xlsx に対応する。

## 8. ディレクトリ構成（案）

```
src/
  main.ts
  domain/
    types.ts      正規化データモデル
    score.ts      総合点 → 研究室選好リスト
    solve.ts      stableMatch 呼び出し
    report.ts     集計・安定性検証
  io/
    eclass.ts           eClass テキスト → 希望順位
    gpaWorkbook.ts      Excel → GPA
    labWorkbook.ts      Excel → 裁量点
    export.ts           CSV / xlsx 出力
  ui/
    ...
```

ブラウザビルドでは `hospitalResident.ts` が `node:fs` / `node:path` を import するため、
Vite の `resolve.alias` でスタブに逃がす（`examples/web/vite.config.ts` に前例あり）。
なお実データの配属で使うのは `stableMatch()` のみで、`HospitalResident`（乱数生成）は
テスト用データ作成にのみ使う。

## 9. 実装マイルストーン

- **M0** プロジェクト雛形（Vite + TypeScript + ライブラリ導入、node stub alias）
- **M1** ドメイン層（スコア計算 → stableMatch → 集計）＋ 合成データによるユニットテスト
- **M2** UI 骨組み（D&D、パラメータ、結果表示、CSV 出力）— 合成データで一本通す
- **M3** 実ファイルのパーサ実装（eClass テキスト / GPA Excel / 裁量点 Excel）
- **M4** Excel 出力、安定性検証、統計表示、GitHub Pages 公開

M1・M2 は実ファイルの形式が未確定でも進められる。

## 10. 未確定事項

1. 研究室の一覧・定員をどう用意するか（今は CSV。Excel でも読めるが見本は CSV）
2. 定員を画面から上書きできるようにするか（6 節）
